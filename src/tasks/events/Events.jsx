const Events = () => {
    const checkIfPalindrome = () => {

    }

    return (
        <input
            type={'text'}
            onChange={checkIfPalindrome}
        />
    )
}

export default Events;